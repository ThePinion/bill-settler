# you can run with docker compose using
```shell
docker compose up --build
```

# you can run inside minikube using
```shell
minikube start --driver=docker --cpus 4 --memory 4096 --addons ingress
```
```shell
skaffold run
```
